import {
  PrismaPasswordHistoryRepository,
  passwordHistoryRepository,
} from '../password-history.repository';

// Re-export the implementation class
export { PrismaPasswordHistoryRepository };

// Export the singleton instance
export { passwordHistoryRepository };
